import React from 'react';
import { connect } from 'react-redux';

const Gallery = ({}) => {
    return (
        <div>
            <h2>galleryy</h2>
        </div>
    )
}

export default connect(state => state)(Gallery)